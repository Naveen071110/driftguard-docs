# Quickstart

<Info>
  You'll need an API key to follow this guide.
  [Request early access →](https://driftguard.dev/#waitlist)
</Info>

## Step 1: Get your API key

After signing up, your API key is available in your dashboard.
Store it as an environment variable — never hardcode it.

```bash
export DRIFTGUARD_KEY="dg_live_xxxxxxxxxxxx"
